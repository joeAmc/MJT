import React from "react";
import HeaderType from "./HeaderType";

const Header = () => {
  return (
    <div className="header bg-white flex justify-left relative shadow-sm transition h-24">
      <HeaderType />
    </div>
  );
};

export default Header;
